{
  "name": "r2-uploader",
  "version": "1.1.0",
  "description": "Remote URL to Cloudflare R2 uploader with progress + optional MP4 faststart",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.645.0",
    "@aws-sdk/lib-storage": "^3.645.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
